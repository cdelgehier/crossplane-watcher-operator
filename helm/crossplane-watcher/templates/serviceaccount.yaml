{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "crossplane-watcher.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "crossplane-watcher.labels" . | nindent 4 }}
{{- end }}
